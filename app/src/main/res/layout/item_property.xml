<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingLeft="@dimen/item_property_horizontal_padding"
    android:paddingRight="@dimen/item_property_horizontal_padding"
    android:paddingTop="@dimen/item_property_vertical_padding"
    android:paddingBottom="@dimen/item_property_vertical_padding">

    <ImageView
        android:id="@+id/item_property_image"
        android:layout_width="@dimen/item_property_icon_size"
        android:layout_height="@dimen/item_property_icon_size"
        app:srcCompat="@drawable/ic_property"
        android:tint="@color/blue"
        tools:ignore="ContentDescription"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/item_property_name"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:gravity="start"
                android:paddingLeft="@dimen/item_property_horizontal_padding"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_default"
                tools:text="@string/property" />

            <TextView
                android:id="@+id/item_property_percent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_default"
                tools:text="XX %" />
        </LinearLayout>

        <SeekBar
            android:id="@+id/item_property_seek_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>

</LinearLayout>